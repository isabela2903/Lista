<Window x:Class="Lista.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Width="400">
            <Label Content="Digite um nome: " />
            <TextBox x:Name="tbNome" />
            <Button Content="Adicionar nome na lista" Margin="0 10 0 5" Click="BtnAdicionaNome_OnClick" />
            <Button Content="Remover um nome da lista" Click="BtnRemoveNome_OnClick" Margin="0 0 0 5" />
            <Button Content="Encontrar nomes na lista" Click="BtnEncontrarNomes_OnClick" />
        </StackPanel>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Right" Width="360">
            <ListBox x:Name="lbNomes" ItemsSource="{Binding nomes}" SelectionMode="Multiple" BorderThickness="0" />
        </StackPanel>
    </Grid>
</Window>